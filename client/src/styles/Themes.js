import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#000000', // Replace with your primary color value
      contrastText: '#ffffff', // Replace with your primary contrast text color value
    },
    // Define other palette colors as needed
  },
});

export default theme;
